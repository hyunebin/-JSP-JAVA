Super는 부모를 찾는 클래스
오버라이드의 장점은 잘 만들어진 클래스를 상속받아 일부 메소드를 수정할 수 있다는 것 입니다.

자식클래스에서 부모클래스를 오버라이드하면 부모클래스를의 메소드를 사용할 수가 없습니다.이것이 오라이드의 단점이라고 할 수 있다.

자식클래스에서 부모클래스의 메소드를 사용하고자 할 때 super키워드를 사용한다.

Super를 만난순간 부모클래스로 돌아가 해당 메소드를 실행하고 내려옴



상속할 객체를 생성하면 부모객체가 먼저 생성되고 자식객체가 생성된다.
